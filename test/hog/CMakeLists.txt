if(FFTW3_FOUND)
	INCLUDE_DIRECTORIES(${FFTW3_INCLUDE_DIR})
	INCLUDE_DIRECTORIES(${FFTW3F_INCLUDE_DIR})
	INCLUDE_DIRECTORIES(${FFTW3L_INCLUDE_DIR})

	VIGRA_ADD_TEST(test_hog test.cxx LIBRARIES vigraimpex ${FFTW3_LIBRARIES} ${FFTW3F_LIBRARIES} ${FFTW3L_LIBRARIES})
else()
    MESSAGE(STATUS "** WARNING: test_fourier will not be executed")
endif()

