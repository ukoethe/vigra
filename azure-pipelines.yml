# Pipelines are made of one or more jobs and may include resources and variables.
# Jobs are made of one or more steps plus some job-specific data. Steps can be
# tasks, scripts, or references to external templates.

jobs:
  - job: nmake
    pool:
      vmImage: 'vs2015-win2012r2'
    strategy:
      maxParallel: 2
      matrix:
        py36:
          PY_VERSION: 3.6
        # py27:
        #   PY_VERSION: 2.7
    steps:
      - script: |
          echo "running vcvarsall.bat"
          call "c:\Program Files (x86)\Microsoft Visual Studio 14.0\VC\vcvarsall.bat" x64
          echo "done"
          where cl
          echo "%PATH%"
          cl /help
          echo "looking for ctest"
          where ctest
          echo "looking for python"
          where python
          dir C:\Python27
          dir C:\Python27\Tools
          dir C:\Python27\Tools\Scripts
          set PATH=%PATH%;C:\Python27\Tools\Scripts
          echo "installing conda"
          pip install conda
          conda --help
